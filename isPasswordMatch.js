/**
 * Test to see if two passwords are the same.
 * @param  {String} pass        password A
 * @param  {String} confirmPassword password B
 * @return {Bool}              True if they match, otherwise false.
 */
const isPasswordMatch = (password, confirmPassword) => password === confirmPassword

module.exports = isPasswordMatch
