/**
 * Test to see if two values are the same.
 * @param  {String} value        value A
 * @param  {String} value        value B
 * @return {Bool}              True if they match, otherwise false.
 */
const isValuesMatch = (valueA, valueB) => valueA === valueB

module.exports = isValuesMatch
